# npm run 'keyword' 입력시 
missing script: 'keyword' 에러
-> package.json > script에 없는 속성이다.

*** 나같은 경우 Terminal위치가 package.json이 위치한 곳이 아니였다.


* keyword : package.json에서 설정
  - package.json : npm init -y 로 생성
*** Terminal 명령어 입력시 keyword를 설정한 Package.json파일 위치에서 수행해야한다.
 
